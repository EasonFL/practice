define  _CRT_SECURE_NO_WARNINGS	
#include<stdio.h>
int main()
{
	int i = 1;
	int num = 0;
	for (; i < 101; i++)
	{
		if (9 == i % 10 || 9 == i / 10)
		{
			if (9 == i % 10 && 9 == i / 10)
				num += 2;
			else num += 1;
		}

	}
	printf("The number of 9 = %d \n", num);

}
